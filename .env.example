# AfiaCare AI Service Configuration

# Server-side OpenAI API key for speech and transcription routes.
# Keep this server-side only. Do not prefix with NEXT_PUBLIC_.
# OPENAI_API_KEY=sk-...

# Optional public feature flag. Enable only after the server-side AI flow is wired.
# NEXT_PUBLIC_ENABLE_SERVER_AI=false
